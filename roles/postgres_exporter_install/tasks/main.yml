- name: Create node_exporter user
  user:
    name: "{{ postgres_exporter_user }}"
    shell: /sbin/nologin
    system: true

- name: Download node_exporter
  get_url:
    url: "https://github.com/prometheus-community/postgres_exporter/releases/download/v{{ postgres_exporter_version }}/postgres_exporter-{{ postgres_exporter_version }}.linux-amd64.tar.gz"
    dest: "/tmp/postgres_exporter.tar.gz"

- name: Extract node_exporter
  unarchive:
    src: /tmp/postgres_exporter.tar.gz
    dest: /tmp/
    remote_src: yes

- name: Copy binary
  copy:
    src: "/tmp/postgres_exporter-{{ postgres_exporter_version }}.linux-amd64/postgres_exporter"
    dest: /usr/local/bin/postgres_exporter
    mode: '0755'
    remote_src: yes

- name: Create systemd service
  template:
    src: postgres_exporter.service.j2
    dest: /etc/systemd/system/postgres_exporter.service
  notify: restart postgres_exporter

- name: Create env file for connect
  template: 
    src: env_file.j2
    dest: /home/user/postgres-exporter-PG1.env

- name: Copy queries file to var folder
  copy:
    src: queries.yaml
    dest: /var/queries.yaml
    
- name: Enable and start postgres_exporter
  systemd:
    name: postgres_exporter
    enabled: true
    state: started
    daemon_reload: true