- name: Generate targets file
  template:
    src: targets.json.j2
    dest: "{{ prometheus_targets_path }}"
    mode: '0644'

- name: Generate main promethus file
  template:
    src: prometheus.yml.j2
    dest: /root/prometheus/etc/prometheus.yml

- name: Restart docker services
  shell: docker restart prometheus grafana